import { GroupByGenrePipe } from './group-by-genre.pipe';

describe('GroupByGenrePipe', () => {
  it('create an instance', () => {
    const pipe = new GroupByGenrePipe();
    expect(pipe).toBeTruthy();
  });
});
